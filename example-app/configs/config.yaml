app:
  name: example-app
  version: 1.0.0

server:
  port: 8080
  mode: debug

log:
  level: info
  encoding: console

database:
  driver: mysql
  source: root:123456@tcp(localhost:3306)/laravel?charset=utf8mb4&parseTime=True&loc=Local
